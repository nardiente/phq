import SectionHeader from '../../SectionHeader';

export function FeedbackHeader() {
  return (
    <SectionHeader
      title="Feedback approval"
      description="Choose which ideas and comments you want to reject or approve on your public board."
    />
  );
}
